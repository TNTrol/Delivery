spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  datasource:
    driverClassName: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/delivery2
    user: tntrol
    password: dfkjd
message:
  topic:
    name: queue1
server:
  port: 16002
mail:
  address: localhost:16000/email
logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
        type:
          descriptor:
            sql:
              BasicBinder: TRACE


